# Auto-RLC-Circuit-Data-Processor

> [!WARNING]
>
> 此项目尚未完工

本项目是物理实验B探究性课题『』的代码仓库，旨在自动化处理RLC电路实验的实验数据，并接入本地小模型分析结果给出报告。

运行代码前，请先安装`requirements.txt`中要求的库：

```bash
pip install -r requirements.txt
```

然后，执行以下命令运行`main.py`：

```bash
streamlit run main.py
```

即可在网页版前端处理数据。